---
interface Props {
  pageTitle?: string;
  metaDescription?: string;
  schemaJsonLd?: string;
}

const { pageTitle: titleProp, metaDescription} = Astro.props;
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

const pageTitle = titleProp ?? 'Vehicle Gallery | Three Piece';
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{pageTitle}</title>
    {metaDescription && (
      <meta name="description" content={metaDescription.replace(/"/g, '&quot;')} />
    )}
  
    <meta name="google-site-verification" content="BguNbBNXmRkDoM059pKt67XRl6AJR7EbTIZdzmrmye4" />
  </head>
  <body>
    <!-- <Header /> -->
    <slot />
    <!-- <Footer /> -->
  </body>
</html>
